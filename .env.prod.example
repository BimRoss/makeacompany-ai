# Copy to `.env.prod` (gitignored). Cluster sync:
#   ./scripts/update-rancher-secrets.sh
#
# Canonical key catalog lives in `.env.example` (production URLs, Stripe live keys, allowlists, Grafana URLs, etc.).

APP_ENV=production
APP_BASE_URL=
NEXT_PUBLIC_SITE_URL=
BACKEND_INTERNAL_SERVICE_TOKEN=
REDIS_URL=
