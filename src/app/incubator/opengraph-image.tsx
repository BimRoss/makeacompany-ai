// The incubator card is now the apex homepage card; re-export it so the
// preview route and the homepage share one source of truth and never drift.
export { alt, contentType, size, default } from "../opengraph-image";
