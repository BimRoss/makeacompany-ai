# Local / compose
APP_BASE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_BACKEND_API_BASE_URL=http://localhost:8080

REDIS_URL=redis://localhost:6379/0

# Stripe (test mode for local)
STRIPE_SECRET_KEY=sk_test_...
# Snapshot destination signing secret (Dashboard → snapshot webhook). Legacy: STRIPE_WEBHOOK_SECRET alone still works as snapshot.
STRIPE_WEBHOOK_SECRET_SNAPSHOT=whsec_...
# Thin destination signing secret (same HTTP path as snapshot; different whsec).
STRIPE_WEBHOOK_SECRET_THIN=whsec_...
STRIPE_PRICE_ID_WAITLIST_TEST=price_...
STRIPE_PRICE_ID_WAITLIST_LIVE=price_...
